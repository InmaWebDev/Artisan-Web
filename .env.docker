# Configuración para Docker Compose
# Copiar a .env para usarlo con docker-compose

# Base de datos
DB_USER=artisan_user
DB_PASSWORD=CambiarContraseñaSegura123!
DB_NAME=artisan_db

# Flask
SECRET_KEY=tu_clave_secreta_cambiar_en_produccion_1234567890abcdef
WHATSAPP_PHONE=573123580705
DATABASE_URL=postgresql://artisan_user:CambiarContraseñaSegura123!@db:5432/artisan_db
FLASK_ENV=production
FLASK_DEBUG=0
RATELIMIT_STORAGE_URI=memory://

# Admin inicial para docker (opcional)
INITIAL_ADMIN_EMAIL=admin@artisan.com
INITIAL_ADMIN_PASSWORD=AdminDockerPass123!

# Email (opcional si se implementa)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=tu_email@gmail.com
MAIL_PASSWORD=tu_password_aqui
